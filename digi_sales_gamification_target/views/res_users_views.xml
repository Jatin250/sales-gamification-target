<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_users_form_gamification_inherit" model="ir.ui.view">
        <field name="name">res.users.form.gamification</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Sales Gamification" name="sales_gamification">
                    <group>
                        <group string="Target Configuration" groups="sales_team.group_sale_manager">
                            <field name="target_type" widget="radio" options="{'horizontal': true}"/>
                            <field name="target_start_date"/>
                            <field name="target_end_date"/>
                            <field name="target_amount"/>
                        </group>
                        <group string="Current Achievement">
                            <field name="current_sales" readonly="1"/>
                            <field name="target_progress" widget="progressbar"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>